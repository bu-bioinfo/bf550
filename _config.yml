# Site settings
title: BF550 - Applied Statistics & Machine Learning for the Life Sciences
description: Course materials for BF550 at Boston University — applied statistics and machine learning, taught for code literacy in the age of coding agents.
baseurl: "" # the subpath of your site
url: "https://bu-cds-bf550.github.io" # the base hostname & protocol for your site
email: ""

theme: minimal-mistakes-jekyll

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Collections
collections:
  lectures:
    output: true
    permalink: /lectures/:path/
  labs:
    output: true
    permalink: /labs/:path/
  guides:
    output: true
    permalink: /guides/:path/

# Defaults
defaults:
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      sidebar:
        nav: "sidebar"
      classes: wide

  # _lectures
  - scope:
      path: ""
      type: lectures
    values:
      layout: single
      sidebar:
        nav: "sidebar"
      classes: wide

  # _labs
  - scope:
      path: ""
      type: labs
    values:
      layout: single
      sidebar:
        nav: "sidebar"
      classes: wide
      toc: true
      toc_sticky: true

  # _guides
  - scope:
      path: ""
      type: guides
    values:
      layout: single
      sidebar:
        nav: "sidebar"
      classes: wide
      toc: true
      toc_sticky: true

# Site timezone
timezone: America/New_York

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - README.md
  - AI_DISCLOSURE.md
  - .gitignore
  - Dockerfile
  - internal/
  # Instructor-facing material must never be published. `lectures/` is listed because a
  # run-sheet once landed there and shipped; `tools/` because the tripwire lint's own pattern
  # literals would otherwise match themselves in the built output.
  - lectures/
  - tools/
