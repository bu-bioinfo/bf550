# Instructor build: `quarto render slides --profile instructor` (or `tools/build_slides.sh --instructor`).
# Same decks, speaker notes kept, written outside `_site/` so nothing here can be published.
project:
  output-dir: ../_instructor-slides
format:
  revealjs:
    footer: "BF550 · instructor build with speaker notes"
keep-notes: true
